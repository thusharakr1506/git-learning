git config --global user.name "your name"
git config --global user.email "email id"
git list : shows history
git branch :list all branches
git remote add name remote path
git remote -v
git stash
git stash pop
git rebase
git merge
